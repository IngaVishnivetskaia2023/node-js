## Error

### Кто предоставляет объект Error

Три уровня происхождения

```
┌───────────────────────────���─────────────────────────────────────┐
│  Уровень 1: ECMA-262 (спецификация языка JavaScript)            │
│                                                                 │
│  Определяет: Error, TypeError, RangeError, ReferenceError,      │
│  SyntaxError, URIError, EvalError, AggregateError               │
│  Свойства: message, name, cause                                 │
│                                                                 │
│  Реализует: JavaScript ENGINE (V8, SpiderMonkey, JavaScriptCore)│
├─────────────────────────────────────────────────────────────────┤
│  Уровень 2: ПЛАТФОРМА (runtime)                                 │
│                                                                 │
│  Node.js добавляет:          Browser добавляет:                 │
│  - SystemError               - DOMException                     │
│  - error.code ('ENOENT')     - ErrorEvent                       │
│  - error.errno               - error.filename                   │
│  - error.syscall             - error.lineno                     │
│  - AbortError                - AbortError                       │
├─────────────────────────────────────────────────────────────────┤
│  Уровень 3: ПОЛЬЗОВАТЕЛЬСКИЙ КОД                               │
│                                                                 │
│  class MyError extends Error { ... }                            │
└─────────────────────────────────────────────────────────────────┘
```

Ключевой факт: и Chrome, и Node.js используют один и тот же V8 engine. Базовый Error — идентичен. Разница — в том, что платформа добавляет сверху.


#### Что определяет ECMAScript (одинаково ВЕЗДЕ)
```
// Конструкторы — из спецификации ECMA-262
new Error('something went wrong')
new TypeError('expected string, got number')
new RangeError('index out of bounds')
new ReferenceError('x is not defined')
new SyntaxError('Unexpected token')
new URIError('malformed URI')
new EvalError('eval error')             // почти не используется
new AggregateError([err1, err2], 'msg') // ES2021

// Стандартные свойства
const err = new Error('boom');
err.message    // 'boom'           — ES spec
err.name       // 'Error'          — ES spec
err.cause      // undefined        — ES2022: new Error('x', { cause: original })
err.stack      // '...'            — ⚠️ НЕ в спецификации, но реализован везде
```

🐦 `stack` реализован всеми движками, **но его формат не стандартизирован** — нет гарантии парсинга.

#### Свойства объекта Error

| Свойство | ECMAScript | Node.js | Browser (Chrome) | Описание |
| :-- | :-- | :-- | :-- | :-- |
| message | ✅ | ✅ | ✅ | Текст ошибки |
| name | ✅ | ✅ | ✅ | Имя типа ('TypeError', 'Error') |
| cause | ✅ ES2022 | ✅ | ✅ | Оригинальная ошибка-причина |
| stack | ❌ (не в спеке) | ✅ (V8) | ✅ (V8) | Stack trace строкой |
| code | ❌ | ✅ | ❌ | 'ERR_INVALID_ARG_TYPE', 'ENOENT' |
| errno | ❌ | ✅ | ❌ | Числовой код POSIX (-2) |
| syscall | ❌ | ✅ | ❌ | Какой syscall упал ('open', 'read') |
| path | ❌ | ✅ | ❌ | Путь файла (fs-ошибки) |
| address | ❌ | ✅ | ❌ | IP-адрес (net-ошибки) |
| port | ❌ | ✅ | ❌ | Порт (net-ошибки) |
| info | ❌ | ✅ | ❌ | Доп. контекст (TLS, DNS) |
| filename | ❌ | ❌ | ✅ (ErrorEvent) | URL скрипта с ошибкой |
| lineno | ❌ | ❌ | ✅ (ErrorEvent) | Номер строки |
| colno | ❌ | ❌ | ✅ (ErrorEvent) | Номер колонки |


#### Типы ошибок

| Тип | ECMAScript | Node.js | Browser | Когда возникает |
| :-- | :-- | :-- | :-- | :-- |
| Error | ✅ | ✅ | ✅ | Базовый тип |
| TypeError | ✅ | ✅ | ✅ | undefined.prop, неверный тип аргумента |
| RangeError | ✅ | ✅ | ✅ | Число вне диапазона, stack overflow |
| ReferenceError | ✅ | ✅ | ✅ | Необъявленная переменная |
| SyntaxError | ✅ | ✅ | ✅ | Невалидный синтаксис |
| AggregateError | ✅ ES2021 | ✅ | ✅ | Promise.any() — все rejected |
| SystemError | ❌ | ✅ | ❌ | Ошибки ОС (fs, net, dns) |
| AbortError | ❌ | ✅ | ✅ | AbortController.abort() |
| DOMException | ❌ | ✅ (частично) | ✅ | Web API ошибки |


```
ECMAScript Spec (V8 реализует)
├── Error              ← message, name, cause
├── TypeError
├── RangeError
├── ReferenceError
├── SyntaxError
└── AggregateError

Node.js добавляет поверх:
├── error.code         ← 'ENOENT', 'ERR_INVALID_ARG_TYPE'
├── error.errno        ← числовой POSIX код
├── error.syscall      ← 'open', 'read', 'connect'
├── error.path         ← путь файла
├── error.address/port ← сетевые координаты
└── Кодированные ошибки: ERR_* (сотни типов)

Browser добавляет поверх:
├── DOMException       ← ошибки Web API
├── ErrorEvent         ← filename, lineno, colno
├── SecurityError      ← CORS, mixed content
└── Специфичные для API (MediaError, GeolocationPositionError, ...)
```


### Зачем создавать объект Error

❗`Error` — это контейнер **диагностической информации**. 
Его главная ценность — `stack trace`: он фиксирует **точное место в коде, где произошла проблема**.

#### JavaScript позволяет `throw` любое значение:

```
throw 'something broke';          // строка
throw 404;                        // число
throw { status: 'fail' };         // объект
throw new Error('something broke'); // Error объект
```

#### при отладке:
```
// ❌ throw строку
throw 'something broke';

// В catch:
// err = 'something broke'
// err.stack = undefined     ← ГДЕ это произошло? НЕИЗВЕСТНО
// err.message = undefined
// typeof err = 'string'    ← нет методов, нет информации
```

#### при отладке с `Error`:
```
// ✅ throw Error
throw new Error('something broke');

// В catch:
// err.message = 'something broke'
// err.name = 'Error'
// err.stack = 
//   Error: something broke
//       at processPayment (/app/payments.js:47:11)    ← ТОЧНАЯ строка
//       at handleOrder (/app/orders.js:23:5)          ← кто вызвал
//       at Router.handle (/app/router.js:112:7)       ← откуда пришёл запрос
//       at Server.emit (node:events:513:28)           ← весь путь
```

#### Три свойства — три ответа на вопросы

| Свойство | Отвечает на вопрос | Значение |
| :-- | :-- | :-- |
| name | ЧТО за тип проблемы? | 'Error', 'TypeError', 'ValidationError' |
| message | КАКАЯ именно проблема? | 'User not found' |
| stack | ГДЕ это произошло? | Полный call stack с файлами и строками |


### How to Deliver Errors

#### Что значит «доставить» ошибку - у ошибки есть две стороны:
```
ПРОИЗВОДИТЕЛЬ ошибки              ПОТРЕБИТЕЛЬ ошибки
(код который обнаружил проблему)   (код который знает что с ней делать)
         │                                    ▲
         │    КАК ошибка попадает отсюда      │
         └────────────── СЮДА? ──────────────┘

```

#### `throw` — механика на уровне Call Stack

🐦 Выполнение с throw — **stack unwinding**

```
function third()  { throw new Error('boom'); }
function second() { return third(); }
function first()  { return second(); }
first();
```

```
Call Stack:                    throw → UNWINDING (размотка):

┌─────────────┐               
│   third()   │ ← throw!     third(): есть catch? НЕТ → убран, идём ниже
├─────────────┤               ┌─────────────┐
│  second()   │               │  second()   │ есть catch? НЕТ → убран
├─────────────┤               ├─────────────┤
│   first()   │               │   first()   │ есть catch? НЕТ → убран
├─────────────┤               ├─────────────┤
│   <global>  │               │   <global>  │ есть catch? НЕТ → 💀
└─────────────┘               └─────────────┘
                                             uncaughtException → process.exit(1)

```

#### Stack unwinding
— V8 последовательно снимает фреймы со стека, проверяя каждый: «есть ли здесь `try/catch`?». 
Если нет — фрейм уничтожается и V8 переходит к следующему ниже.


#### `try/catch` останавливает unwinding

```
function third()  { throw new Error('boom'); }
function second() { return third(); }
function first()  {
  try {
    return second();
  } catch (err) {
    console.log('Поймал:', err.message);
  }
}
first();
```

```
Call Stack:                    throw → UNWINDING:

┌─────────────┐               
│   third()   │ ← throw!     third(): есть catch? НЕТ → убран
├─────────────┤               ┌─────────────┐
│  second()   │               │  second()   │ есть catch? НЕТ → убран
├─────────────┤               ├─────────────┤
│   first()   │               │   first()   │ есть catch? ДА! ✅
├─────────────┤               ├─────────────┤       │
│   <global>  │               │   <global>  │       ▼
└─────────────┘               └─────────────┘  Выполняем блок catch
                                                err = Error('boom')
                                                console.log(...)
                                                
                                                Нормальный поток ВОССТАНОВЛЕН
                                                first() продолжает выполнение

```

#### Что конкретно происходит при unwinding

Когда фрейм снимается со стека, всё его состояние уничтожается:

```
function processOrder() {
  const connection = db.getConnection();     // ← ресурс захвачен
  const lock = mutex.acquire('orders');      // ← lock захвачен
  
  const user = getUser(userId);              // ← throw внутри!
  
  lock.release();                            // ← НЕ выполнится!
  connection.close();                        // ← НЕ выполнится!
  return result;                             // ← НЕ выполнится!
}
```

```
processOrder() снят со стека при unwinding:
  - connection — утечка (не закрыт)
  - lock — утечка (не отпущен)
  - Код после throw — НИКОГДА не выполнится
```

#### Поэтому используют `finally`:

```
function processOrder() {
  const connection = db.getConnection();
  try {
    const result = riskyOperation();
    return result;
  } catch (err) {
    // обработка ошибки
    throw err;  // можно перебросить дальше
  } finally {
    // ВСЕГДА выполнится — и при return, и при throw
    connection.close();  // ✅ ресурс освобождён в любом случае
  }
}
```


#### `throw` vs `return` — два способа выйти из функции

```
                    RETURN                         THROW
              ─────────────────              ─────────────────
Передаёт:     значение (результат)          Error (проблему)
Куда:          вызывающей функции            ближайшему catch (может быть далеко)
Поток:         нормальный                    аварийный (unwinding)
Промежуточные  выполняют код                 ПРОПУСКАЮТСЯ
функции:       после вызова                  (снимаются со стека)
```

```
// return — передача ВВЕРХ на один уровень
function getUser() {
  return { name: 'John' };  // → вызывающий получит значение
}

// throw — «прыжок» через ЛЮБОЕ количество уровней
function validateInput() {
  throw new Error('invalid');  // → может поймать catch в 5 уровнях выше
}
```


## Какой паттерн обработки ошибок использовать в Node.js

#### Три паттерна — историческая эволюция

```
2009 ──────── 2012 ──────── 2015 ──────── 2017 ──────── сейчас
Node 0.1       Node 0.8      Node 4        Node 8        Node 22

Error-first    Streams/       Promises      async/await    throw + try/catch
callbacks      EventEmitter   (.then)       (рекомендуемый)
(единственный  'error' event
способ)
```

Каждый следующий паттерн появлялся потому что предыдущий имел проблемы.

### 1. `throw` + `try/catch` — рекомендуемый дефолт

🐦 Синхронный код
```
function parseConfig(raw) {
  if (typeof raw !== 'string') {
    throw new TypeError('Config must be a string');
  }
  
  const parsed = JSON.parse(raw);  // может throw SyntaxError
  
  if (!parsed.port) {
    throw new Error('Config missing required field: port');
  }
  
  return parsed;
}

try {
  const config = parseConfig(rawInput);
  startServer(config);
} catch (err) {
  console.error('Failed to start:', err.message);
  process.exit(1);
}
```

🐦 Асинхронный код (async/await)

```
async function fetchUser(id) {
  const res = await fetch(`https://api.example.com/users/${id}`);
  
  if (!res.ok) {
    throw new Error(`API error: ${res.status} ${res.statusText}`);
  }
  
  const user = await res.json();  // может throw если невалидный JSON
  
  if (!user.email) {
    throw new Error(`User ${id} has no email`);
  }
  
  return user;
}

// Вызывающий код — ТОТ ЖЕ try/catch работает для async:
async function handleRequest(req, res) {
  try {
    const user = await fetchUser(req.params.id);
    const orders = await fetchOrders(user.id);
    const invoice = generateInvoice(orders);  // sync — тоже поймается
    res.json(invoice);
  } catch (err) {
    // Ловит ВСЁ: и sync throw, и rejected promise от await
    console.error(err);
    res.status(500).json({ error: 'Internal server error' });
  }
}
```

#### Почему это работает единообразно

```
async function example() {
  throw new Error('boom');
}

// async function ВСЕГДА возвращает Promise
// throw внутри async → эквивалент Promise.reject(new Error('boom'))
// await этого промиса → throw восстанавливается в вызывающем коде

try {
  await example();  // throw внутри → rejection → await пробрасывает как throw
} catch (err) {
  // пойман — один механизм для sync и async
}
```

```
SYNC:   throw → stack unwinding → catch
ASYNC:  throw → rejection → await → throw → stack unwinding → catch
                                     ↑
                            await "конвертирует" rejection
                            обратно в throw
```



### 2. Error-first callbacks — legacy паттерн

```
const fs = require('fs');

// callback: первый аргумент ВСЕГДА ошибка (или null)
fs.readFile('/etc/passwd', 'utf8', (err, data) => {
  if (err) {
    console.error('Read failed:', err.message);
    return;  // ОБЯЗАТЕЛЬНО return — иначе код продолжит выполняться !!!
  }
  console.log(data);
});
```

### 3. EventEmitter 'error' event — для долгоживущих объектов

❓ Почему нужен отдельный паттерн
Streams, серверы, соединения с БД — **они живут долго и могут выдать ошибку в любой момент**, не привязанный к конкретному вызову:

```
// TCP сервер — работает часами
// Ошибка может прийти в ЛЮБОЙ момент: через 1 секунду или через 3 часа
// Нет конкретного await/callback куда вернуть ошибку
const server = net.createServer();
server.listen(3000);

// Через 2 часа: клиент отправил malformed данные → ошибка
// Куда её "доставить"? Нет pending callback/promise для неё
// → Event: server.emit('error', err)
```

#### Как обрабатывать

```
import { createReadStream } from 'node:fs';
import { createServer } from 'node:net';

// Streams
const stream = createReadStream('/path/to/file');
stream.on('error', (err) => {
  // Обязательно! Без этого — uncaughtException → process crash
  console.error('Stream error:', err.message);
});

// TCP server
const server = createServer();
server.on('error', (err) => {
  if (err.code === 'EADDRINUSE') {
    console.error(`Port ${port} already in use`);
    process.exit(1);
  }
});

// Database connection
const connection = await db.connect();
connection.on('error', (err) => {
  // Соединение разорвалось через час работы
  console.error('DB connection lost:', err.message);
  reconnect();
});
```

## Сводка: когда что использовать

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   Пишешь НОВЫЙ код?                                            │
│        │                                                        │
│        ├── Синхронная функция → throw + try/catch              │
│        │                                                        │
│        ├── Асинхронная функция → async/await + try/catch       │
│        │                                                        │
│        └── Долгоживущий объект (stream, server, connection)     │
│                → EventEmitter .on('error')                      │
│                                                                 │
│   Работаешь со СТАРЫМ API?                                     │
│        │                                                        │
│        ├── Есть вариант из node:*/promises? → используй его    │
│        │   (fs/promises, timers/promises, stream/promises)      │
│        │                                                        │
│        ├── Нет промис-варианта? → util.promisify(fn)           │
│        │                                                        │
│        └── Нельзя промисифицировать? → error-first callback    │
│            (if (err) { handle })                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```


## В браузере

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   Пишешь НОВЫЙ код?                                            │
│        │                                                        │
│        ├── Синхронная логика → throw + try/catch               │
│        │                                                        │
│        ├── Async операция (fetch, API) → async/await + try/catch│
│        │                                                        │
│        ├── React компонент (render safety) → Error Boundary    │
│        │                                                        │
│        ├── React event handler → try/catch ВНУТРИ handler      │
│        │                                                        │
│        ├── Долгоживущее соединение (WS) → .onerror + .onclose  │
│        │                                                        │
│        └── Загрузка ресурса (img, script, chunk)               │
│                → onerror / .catch() на dynamic import           │
│                                                                 │
│   Глобальная страховка (ВСЕГДА добавляй):                      │
│        │                                                        │
│        ├── window.addEventListener('error')                     │
│        ├── window.addEventListener('unhandledrejection')        │
│        └── Корневой Error Boundary вокруг <App />              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```






























































